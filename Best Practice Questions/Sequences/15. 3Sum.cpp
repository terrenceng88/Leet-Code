/*
-----QUESTION-----
Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that i != j, i != k, and j != k, and nums[i] + nums[j] + nums[k] == 0.

Notice that the solution set must not contain duplicate triplets.
*/

//Runs in O(n) time using partial_sum and finding product of all values to the left and right of i

class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result; //vector of triples to be returned
        vector<int> triple; //vector to hold 1 triple
        int a, b, c; //holds the 3 numbers to be added to the triplet
        
        
        
        return result;
    }
};